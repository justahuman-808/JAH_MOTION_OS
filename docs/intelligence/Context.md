# Context.md — Working memory

Short-lived notes for the **active** smoke test or client sprint. Mirror material changes into `state.log` (JSONL) so commits stay compliant.

## Current session

- **Project:** (set in `.env` `MOTION_OS_AE_PROJECT`)
- **Comp:** `MOTION_OS_COMP_NAME`
- **Target layer:** `MOTION_OS_TARGET_LAYER`
- **Property / key:** `MOTION_OS_DEFAULT_PROPERTY` / `MOTION_OS_DEFAULT_KEY_INDEX`

## Overrides

Document any deviation from `.env` here during debugging (Executor should still prefer env for automation).

## Last Observer verdict

- (pending)
