{
  "name": "Elastic Bounce",
  "category": "motion",
  "description": "A very clean and bouncy elastic spring motion for fast UI elements.",
  "tags": ["bounce", "elastic", "spring", "ui"],
  "parameters": {
    "amplitude": 0.05,
    "frequency": 2.0,
    "decay": 5.0
  },
  "code": "amp = {{amplitude}};\nfreq = {{frequency}};\ndecay = {{decay}};\nt = time - inPoint;\nstartVal = [100,100];\nif (numKeys > 0){ \n  n = 0; \n  if (numKeys > 0){\n    n = nearestKey(time).index;\n    if (key(n).time > time){n--;}\n  }\n  if (n == 0) {t = 0;} else {t = time - key(n).time;}\n  if (n > 0){v = velocityAtTime(key(n).time - thisComp.frameDuration/10);} else {v = 0;}\n  value + v*amp*Math.sin(freq*t*2*Math.PI)/Math.exp(decay*t);\n} else {value}"
}
